This script recursively searches directories and files in defined location and prints the sorted file list.
You can also perform actions (copy, move, delete, archive) on listed files using its module.
